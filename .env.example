# ProfFlow environment variables (do not put secrets in source control)

# Required
GEMINI_API_KEY=

# Optional
PROFFLOW_DATA_DIR=~/ProfFlow/data
# Comma-separated list, e.g. http://localhost:3000,http://localhost:3001
PROFFLOW_ALLOWED_ORIGINS=http://localhost:3000
